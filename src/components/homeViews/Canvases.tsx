import { IonContent } from "@ionic/react";
import { ReactNode } from "react";

export default function Canvases(): ReactNode {
    return (
        <IonContent fullscreen>
            
        </IonContent>
    );
}